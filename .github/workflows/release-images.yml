name: Release Images

on:
  workflow_dispatch:

jobs:
  manual-placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Manual release placeholder
        run: echo "Release image publishing is intentionally disabled in this branch"

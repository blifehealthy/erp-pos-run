name: CI

on:
  push:
  pull_request:

jobs:
  register-workflow:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Register workflow
        run: echo "CI workflow registered on default branch"
